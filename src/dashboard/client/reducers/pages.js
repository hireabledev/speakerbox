const initialState = {
  stream: {},
  schedule: {},
  metrics: {},
  settings: {},
};

export default function rssFeedsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
